@{
    ViewData["Title"] = "ParkingLots Manager";
}
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" />
<link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Rounded:opsz,wght,FILL,GRAD@24,400,0,0" />
<link rel="stylesheet" href="~/css/styles.css" />

<div class="id-scene">
    <!-- Header (Search Bar) -->
    <div class="search-bar">
        <input type="text" id="searchBox" class="form-control" placeholder="Tìm kiếm trên Goong Maps" />
        <div class="search-icons">
            <i class="fas fa-search"></i>
            <i class="fas fa-directions"></i>
        </div>
    </div>

    <!-- Sidebar + Main Content -->
    <div class="d-flex">
        <!-- Sidebar -->
        <aside class="sidebar">
            <!-- Header -->
            <header class="sidebar-header">
                <a href="#" class="header-logo">
                    <img src="logo.png" alt="Logo">
                </a>
                <button class="toggler sidebar-toggler">
                    <span class="material-symbols-rounded">chevron_left</span>
                </button>
                <button class="toggler menu-toggler">
                    <span class="material-symbols-rounded">menu</span>
                </button>
            </header>

            <!-- Navigation -->
            <nav>
                <ul class="sidebar-nav">
                    <li class="nav-item">
                        <a href="#" class="nav-link">
                            <span class="navbar-icon material-symbols-rounded">bookmark</span>
                            <span class="nav-label">Bookmark</span>
                        </a>
                        <span class="nav-tooltip">Bookmark</span>
                    </li>
                    <li class="nav-item">
                        <a href="#" class="nav-link">
                            <span class="navbar-icon material-symbols-rounded">history</span>
                            <span class="nav-label">History</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a href="#" class="nav-link">
                            <span class="navbar-icon material-symbols-rounded">notifications</span>
                            <span class="nav-label">Notifications</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a href="#" class="nav-link">
                            <span class="navbar-icon material-symbols-rounded">group</span>
                            <span class="nav-label">Team</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a href="#" class="nav-link">
                            <span class="navbar-icon material-symbols-rounded">analytics</span>
                            <span class="nav-label">Analytics</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a href="#" class="nav-link">
                            <span class="navbar-icon material-symbols-rounded">bookmark</span>
                            <span class="nav-label">Bookmarks</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a href="#" class="nav-link">
                            <span class="navbar-icon material-symbols-rounded">settings</span>
                            <span class="nav-label">Settings</span>
                        </a>
                    </li>
                </ul>
            </nav>
        </aside>

        <!-- Main Content -->
        <div class="map-container">

            <div id='map' style='width: 400px; height: 300px;'></div>
            <script>
                goongjs.accessToken = 'yIjIEaQWjb9xRhAcXW7T1ilJSTjmoAFl1u4XjKSH';
                var map = new goongjs.Map({
                    container: 'map',
                    style: 'https://tiles.goong.io/assets/goong_map_web.json', // stylesheet location
                    center: [105.83991, 21.02800], // starting position [lng, lat]
                    zoom: 9 // starting zoom
                });
            </script>
        </div>
    </div>
</div>

<script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/js/bootstrap.bundle.min.js"></script>

<!-- Goong Maps API -->
<script>
    // Lấy các phần tử cần thiết
    const sidebar = document.querySelector(".sidebar");
    const sidebarToggler = document.querySelector(".sidebar-toggler");
    const menuToggler = document.querySelector(".menu-toggler");

    // Các biến lưu trạng thái
    const collapsedSidebarHeight = "56px";
    const fullSidebarHeight = "100vh";
    const sidebarWidthCollapsed = "80px";
    const sidebarWidthExpanded = "300px";

    // Hàm để chuyển đổi trạng thái của sidebar
    const toggleSidebar = () => {
        sidebar.classList.toggle("active");
        if (sidebar.classList.contains("active")) {
            sidebar.style.width = sidebarWidthCollapsed;
        } else {
            sidebar.style.width = sidebarWidthExpanded;
        }
    };

    // Hàm để chuyển đổi menu
    const toggleMenu = (isMenuActive) => {
        sidebar.style.height = isMenuActive ? `${sidebar.scrollHeight}px` : collapsedSidebarHeight;
        menuToggler.querySelector("span").innerText = isMenuActive ? "close" : "menu";
    };

    // Xử lý sự kiện nhấn nút đóng/mở sidebar
    sidebarToggler.addEventListener("click", () => {
        toggleSidebar();
    });

    // Xử lý sự kiện nhấn nút mở/đóng menu
    menuToggler.addEventListener("click", () => {
        sidebar.classList.toggle("menu-active");
        toggleMenu(sidebar.classList.contains("menu-active"));
    });

    // Xử lý sự kiện thay đổi kích thước cửa sổ
    window.addEventListener("resize", () => {
        if (window.innerWidth >= 1024) {
            sidebar.style.height = fullSidebarHeight;
        } else {
            sidebar.classList.remove("active");
            sidebar.style.height = "auto";
            toggleMenu(sidebar.classList.contains("menu-active"));
        }
    });

    // Sự kiện tìm kiếm
    document.querySelector('.search-icons .fa-search').addEventListener('click', () => {
        const query = document.getElementById('searchBox').value;
        if (query) {
            alert(`Tìm kiếm: ${query}`);
        }
    });
</script>


